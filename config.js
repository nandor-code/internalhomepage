class homePageConfig 
{
    constructor()
    {
        this.headerColor = "lightgray";          // color of the page heading.
        this.serviceWidth = "100%";              // width of the services inside the center gray box.
        this.serviceHeight = "45px";             // height of each service.
        this.serviceBGColor = "darkslateblue";   // background color of each service.
        this.serviceLinkColor = "white";         // text color of service links.
        this.bgColor = "black";                  // page bg color - only if no image is used.
        this.bgImage = "./dark-texture-bkg.jpg"; // page background image.
    }
}

